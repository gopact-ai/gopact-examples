# Generated Agent Quickstart

Run the core `gopact agent init` scaffold generator, then test the generated A2A HTTP agent, its `gopact agent run` path, and its `/agents.json` registry endpoint.

```bash
go run ./quickstart/generated-agent
```
