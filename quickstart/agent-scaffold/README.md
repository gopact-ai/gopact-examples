# Agent Scaffold

Minimal no-credential agent skeleton with graph nodes, checkpointing, approval interrupt, and resume.

```bash
go run ./quickstart/agent-scaffold
```

This is the smallest local scaffold path before adding a real provider. Use `quickstart/react-agent`, `quickstart/plan-exec`, and provider quickstarts to expand it.
